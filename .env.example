# Public content API. The deployed CMS URL is used by default when this is omitted.
POCKETBASE_URL=https://kasiservices.ryucode.site

# Required only for POST /api/inquiries. Keep these values server-only.
PB_SUPERUSER_EMAIL=
PB_SUPERUSER_PASSWORD=
